spring:
  application:
    name: project-os-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/storage
    username: user
    password: 1234
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

logging:
  level:
    org.flywaydb: DEBUG